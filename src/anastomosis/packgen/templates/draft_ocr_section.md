
## OCR evidence (read this second)

${caveat}

- Sample pages by kind: ${classes}
- Recognized tokens: ${token_count}
  (${accepted_count} used, ${below_count} below threshold)
- Held for review, unresolved: ${duplicate_count} duplicate(s) of native
  text, ${disagreement_count} native/OCR disagreement(s)
- Full detail, page classes and the engine manifest: `${evidence_name}`
- Strings marked `[OCR]` in `${unplaced_name}` came from recognition
